; Copy to build_config.cfg (gitignored) and fill in real values.
; CI injects this file at build time. Debug builds IGNORE the live ids
; entirely and force test ads — see core/Cfg.gd.

[admob]
app_id      = ""
banner_id   = ""
rewarded_id = ""

[build]
; Optional override. Leave false; debug builds force test ads regardless.
force_test_ads = false
